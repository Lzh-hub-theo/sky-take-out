<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">
    <select id="listWithCategoryName" resultType="com.sky.vo.SetmealVO">
        select s.id, s.category_id, s.name, s.price, s.status, s.description, s.image, s.update_time, c.name categoryName
        from setmeal s left join category c on s.category_id=c.id
        <where>
            <if test="name!=null"> s.name like concat('%',#{name},'%') </if>
            <if test="categoryId!=null"> category_id = #{categoryId} </if>
            <if test="status!=null"> status = #{status} </if>
        </where>
        order by s.update_time desc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id, name, price, status, description, image)
        values (#{categoryId},#{name},#{price},#{status},#{description},#{image})
    </insert>

    <update id="modify">
        update setmeal
            <set>
            <if test="categoryId != null"> category_id = #{categoryId}, </if>
            <if test="name != null"> name=#{name}, </if>
            <if test="price != null"> price=#{price}, </if>
            <if test="status != null"> status=#{status}, </if>
            <if test="description != null"> description=#{description}, </if>
            <if test="image != null"> image=#{image} </if>
            </set>
        where id = #{id}
    </update>

    <delete id="deleteBatch">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")"> #{id} </foreach>
    </delete>
</mapper>
